import "./ipc";

import React from "react";
import ReactDOM from "react-dom";

import ViewRoot from "./ViewRoot";


ReactDOM.render(
    React.createElement(ViewRoot),
    document.getElementById("app")
);
